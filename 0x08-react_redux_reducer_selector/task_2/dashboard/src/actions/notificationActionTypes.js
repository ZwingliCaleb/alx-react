// action types to make notifications as read and and set type filters as well
export const MARK_AS_READ = 'MARK_AS_READ';
export const SET_TYPE_FILTER = 'SET_TYPE_FILTER';

// Define the filter states for notification types
export const NotificationTypeFilters = {
    DEFAULT: 'DEFAULT',
    URGENT: 'URGENT',
};